# MCP 智能对话助手

一个基于 React 和 TypeScript 的智能对话应用，集成了 Mastra MCP（Model Context Protocol）框架，支持多种类型的智能代理服务。

## 🚀 技术栈

- **前端框架**: React 19.1.0
- **语言**: TypeScript
- **样式**: Tailwind CSS 4.1.10 + Material-UI 7.1.2
- **构建工具**: Webpack 5 + SWC
- **包管理器**: pnpm
- **智能代理**: Mastra MCP Framework
- **图形查询**: Apollo Client + GraphQL
- **代码质量**: ESLint + Prettier + Husky
- **测试**: Jest + BackstopJS

## ✨ 主要功能

- 💬 **智能对话界面**: 基于 Material-UI 的现代化聊天界面
- 🤖 **多代理支持**: 集成天气查询、代码检查、通用路由等多种智能代理
- 🌟 **自动路由**: 根据用户输入内容自动选择合适的代理
- 📱 **响应式设计**: 支持深色主题的移动端友好界面
- ⚡ **实时对话**: 支持流式响应和实时消息更新
- 🎨 **Markdown 渲染**: 支持富文本消息展示

## 📦 环境要求

- Node.js (推荐 20.x 或更高版本)
- pnpm 10+

## 🛠️ 安装

```bash
# 克隆项目
git clone <your-repo-url>
cd my-mcp-agent

# 安装依赖
pnpm install
```

## 🚀 运行项目

### 开发环境

```bash
# 启动开发服务器（带热重载）
pnpm run client:serve

# 构建开发版本
pnpm run client:dev

# 构建生产版本
pnpm run client:prod
```

开发服务器默认在 `http://localhost:3003` 启动。

### 生产构建

```bash
# 构建生产版本
pnpm run client:prod
```

构建后的文件将输出到 `dist/` 目录。

## 🧪 测试

```bash
# 运行单元测试
pnpm run jest:test

# 启动测试 UI 界面
pnpm run jest:ui
```

## 🤖 MCP 代理集成

项目集成了 Mastra MCP 框架，支持以下智能代理：

### 代理类型

- **天气代理 (weatherAgent)**: 处理天气相关查询
- **代码检查代理 (codeCheckAgent)**: 提供代码分析和检查服务
- **路由代理 (routerAgent)**: 处理通用对话和路由任务

### 自动路由逻辑

系统会根据用户输入自动选择合适的代理：

```typescript
// 天气查询示例
"今天天气怎么样？" → weatherAgent

// 代码相关查询示例
"帮我检查这段代码" → codeCheckAgent

// 通用查询示例
"你好" → routerAgent
```

## 🎨 样式系统

- **Tailwind CSS 4.1.10**: 原子化 CSS 框架
- **Material-UI 7.1.2**: React 组件库
- **深色主题**: 预配置的深色模式支持
- **响应式设计**: 移动端优先的设计理念

## 🔧 代码质量

项目配置了完整的代码质量工具链：

### ESLint & Prettier

```bash
# 检查代码风格（注意：项目中未配置 lint 脚本）
# 建议添加: "lint": "eslint src --ext .ts,.tsx"
```

### Git Hooks (Husky)

- **pre-commit**: 自动运行代码检查和格式化
- **commit-msg**: 检查提交信息格式

## 📁 项目结构

```
src/
├── components/          # React 组件
│   ├── ChatGPTComp.tsx # ChatGPT 组件（备用）
│   └── McpComp.tsx     # 主要的 MCP 对话组件
├── pages/              # 页面组件
│   └── App.tsx         # 应用主组件
├── utils/              # 工具函数
│   └── muiTheme.ts     # Material-UI 主题配置
├── client.ts           # Apollo GraphQL 客户端配置
├── index.tsx           # 应用入口点
└── index.css           # 全局样式
```

## 🌐 API 集成

项目连接到 Mastra MCP 服务：

- **生产环境**: `https://my-mastra-app-production.harlanhai7023.workers.dev`
- **本地开发**: `http://localhost:4111` （需要本地启动 MCP 服务）

## 🔨 开发工具配置

### VSCode 推荐插件

- ESLint
- Prettier - Code formatter
- Tailwind CSS IntelliSense
- TypeScript and JavaScript Language Features

### 推荐的 VSCode 设置

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

## 📝 开发规范

### 提交信息规范

使用约定式提交格式：

```
feat: 添加新功能
fix: 修复bug
docs: 更新文档
style: 代码格式化
refactor: 重构代码
test: 添加测试
chore: 构建过程或辅助工具的变动
```

### 代码风格

- 使用 TypeScript 进行类型检查
- 遵循 Airbnb TypeScript 代码规范
- 使用 Prettier 进行代码格式化
- 组件使用函数式组件和 Hooks

## 🐛 故障排除

### 常见问题

1. **安装依赖失败**
   ```bash
   # 清除缓存后重新安装
   pnpm store prune
   pnpm install
   ```

2. **MCP 服务连接失败**
   - 检查网络连接
   - 确认 MCP 服务地址是否正确
   - 查看浏览器控制台错误信息

3. **构建失败**
   ```bash
   # 清理构建缓存
   rm -rf dist/
   pnpm run client:prod
   ```

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 ISC 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 创建 Issue
- 发送邮件到：[harlanhxh@gmail.com]

---

**注意**: 本项目主要用于演示 MCP（Model Context Protocol）框架的集成和使用，展示了如何构建一个现代化的智能对话应用。